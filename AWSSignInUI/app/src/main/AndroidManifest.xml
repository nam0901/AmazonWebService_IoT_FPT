<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.trithang.awssigninui">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DangNhapActitvity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".DangKyActivity"
            android:parentActivityName=".TrangChuActivity">

        </activity>

        <activity android:name=".TrangChuActivity">

        </activity>

        <activity android:name=".XacNhanActivity"
            android:parentActivityName=".TrangChuActivity"
            >

        </activity>

        <activity android:name=".QuenMatKhauActivity">

        </activity>

        <activity android:name=".QuenMatKhauTenDNActivity">

        </activity>
    </application>

</manifest>